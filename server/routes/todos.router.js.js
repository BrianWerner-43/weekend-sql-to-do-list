const router = require('express').Router();
const pool = require('../modules/pool');

// GET ROUTE 
// router.get('/')

// POST route with "task", "location", "complete" ? so the database can call the boolean

// PUT route

// Delete route


module.exports = router;
